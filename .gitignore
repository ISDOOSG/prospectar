# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local MCP config — contém chaves de API, nunca versionar
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- adicionado em 2026-08-31: credencial do banco provisionado na VPS ---
# .pgpass NAO e pego pela trava de segredo (gate_segredos.py) -- o formato
# host:porta:banco:usuario:senha nao bate com nenhum padrao dela. .env.db
# ela pegaria (linha *_SENHA=), mas o .gitignore e a primeira camada, nao a
# unica. NUNCA remover estas duas linhas.
.pgpass
.env.db

